# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.box = "centos/7"
  config.vm.provider "virtualbox" do |v|
    v.memory = 1024
  end
  config.vm.define "consul" do |consul|
    master.vm.network "private_network", ip: "192.168.1.10", virtualbox__intnet: "local"
    master.vm.hostname = "consul"
  end
  config.vm.define "pg1" do |pg1|
    slave.vm.network "private_network", ip: "192.168.1.20", virtualbox__intnet: "local"
    slave.vm.hostname = "pg1"
  end
  config.vm.define "pg2" do |pg2|
    backup.vm.network "private_network", ip: "192.168.1.30", virtualbox__intnet: "local"
    backup.vm.hostname = "pg2"
  end
  config.vm.define "pg3" do |pg3|
    backup.vm.network "private_network", ip: "192.168.1.30", virtualbox__intnet: "local"
    backup.vm.hostname = "pg3"
  end

  config.vm.provision "ansible" do |ansible|
    ansible.verbose = "v"
    ansible.playbook = "provisioning/playbook.yml"
    ansible.become = "true"
  end

end
