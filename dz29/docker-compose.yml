---
version: '3'
services:
  nginx:
    image: nginx:latest
    ports:
      - 80:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/nginx.conf:ro
      - ./php:/var/www/html:rw
    depends_on:
      - fpm

  fpm:
    image: php:7-fpm
    volumes:
      - ./php:/var/www/html





...
